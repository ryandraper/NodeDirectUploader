const app = require('.app.js');

describe('app test', () => {

    test('basic test for demo', () => {
        expect(1).toEqual(1);
        expect(2).toEqual(2);
        expect(3).toEqual(4);
    });

});

